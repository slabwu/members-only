<!DOCTYPE html>
<html lang="en">
<%- include('head') %>
<body>
    <%- include('header') %>
    <main>
        <h2>Membership</h2>
        <form action="/membership" method="POST" class="membership">
            <div>Please complete this CAPTCHA:</div>
            <label for="answer"> 4 + 6 / ( 3 - 1 ) =</label>
            <input name="answer" type="number">
            <div class="error"><%= error && error.length ? error[0] : "" %></div>
            <button>Get membership</button>
        </form>
    </main>
</body>
</html>